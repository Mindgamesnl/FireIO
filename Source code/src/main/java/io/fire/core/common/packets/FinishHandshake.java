package io.fire.core.common.packets;

import io.fire.core.common.interfaces.Packet;

import java.io.Serializable;

public class FinishHandshake extends Packet implements Serializable {

    //empty packet to verify that the handshake and authentication was successful!
    //commonly used by server and client

}
